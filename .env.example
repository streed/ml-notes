# Environment configuration for ml-notes docker-compose setup

# Ollama Configuration (for remote Ollama instance)
OLLAMA_ENDPOINT=http://your-ollama-host:11434

# Model Configuration (pre-configured in containers)
EMBEDDING_MODEL=nomic-embed-text:v1.5
SUMMARIZATION_MODEL=gemma3:4b
CHAT_MODEL_ML_NOTES=gemma3:4b
CHAT_MODEL_LILRAG=gpt-oss:20b

# ml-notes Configuration
ML_NOTES_HOST=0.0.0.0
ML_NOTES_PORT=21212
ML_NOTES_DATA_DIR=/app/data
ML_NOTES_CONFIG_DIR=/app/config

# lil-rag Configuration
LILRAG_HOST=0.0.0.0
LILRAG_PORT=12121
LILRAG_DATA_DIR=/app/data

# Optional: Enable debug mode
DEBUG=false

# Optional: Auto-tagging settings (pre-configured)
ENABLE_AUTO_TAGGING=true
MAX_AUTO_TAGS=5